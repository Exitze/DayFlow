# Dayflow Local Notifications Plan

1. Add tests for notification plan generation from real activities, shifts, and settings.
2. Add a pure Swift notification plan builder to `DayflowCore`.
3. Add an iOS `UserNotifications` scheduler/controller for permission and pending requests.
4. Wire the home bell to open notification settings.
5. Add a notification block to profile/settings.
6. Update legal/support documents for optional on-device notifications.
7. Run `swift test` and an iOS simulator `xcodebuild`.
